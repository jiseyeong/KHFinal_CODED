<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TodayWeather">
	<insert id="insert">
		insert into TodayWeather(TodayWeatherId, AddressId, Recent, Min, Max, WeatherCode)
		 values (TodayWeatherID_SEQ.nextval, #{addressId}, #{recent}, #{min}, #{max}, #{weatherCode})
	</insert>
</mapper>