<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Member">
  	<select id="selectMemberById" resultType="kh.coded.dto.MemberDTO">
  		select * from member where userId = #{userId}
  	</select>
  	<select id="selectMemberByUserNo" resultType="kh.coded.dto.MemberDTO">
  		select * from member where userNo = ${userNo}
  	</select>
  	<insert id="insertMember">
  		<selectKey keyProperty="userNo" resultType="int" order="BEFORE">
  			select member_userno_seq.nextval from dual
  		</selectKey>
  			insert into member(userNo, userId, pw, userNickname, bio, favBrand, address1, address2, role, naverToken, kakaoToken, googleToken)
  			values(${userNo},#{userId},#{pw},#{userNickName},#{bio},#{favBrand},#{address1},#{address2},#{role},#{naverToken},#{kakaoToken},#{googleToken})
  	</insert>
	<delete id="deleteMember">
		delete member where userID = #{userID} and pw = #{pw}
	</delete>
  </mapper>